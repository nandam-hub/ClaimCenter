package gw.surepath.suite.integration.preupdate

/**
 * Priority used to determine ordering for PrioritizedPreUpdateHandler's
 */
enum PreUpdatePriority {
  highest,
  high,
  standard,
  low,
  lowest
}