<input name="${renderId}" type="password"
       class="gw-PasswordValueWidget--valueField gw-noTrack" aria-labelledby="${inputLabelAriaId}" disabled="${disabled}"
       aria-required="${required ? 'true' : 'false'}" aria-invalid="${invalid ? 'true' : 'false'}"
       value="${currentValue}" placeholder="${placeholder}"/>
