<div class="gw-checkboxDiv--checkbox-group">
    <div :for="option" :in="selectedOptions" class="gw-RangeValue ${option.retired ? 'gw-retired'}" data-gw-value="${option.value}">
        ${option.icon}
        <div :if="option.label" class="gw-label">${option.label}</div>
    </div>
</div>